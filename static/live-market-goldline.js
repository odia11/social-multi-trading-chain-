/* Deprecated: the Live Market now uses the single authoritative chart engine
   in live-market-pro.js. Keeping a second SVG renderer here hid the chart that
   owns the real live-price ticker and touch scrub state. Intentionally no-op. */
(function(){'use strict';})();
